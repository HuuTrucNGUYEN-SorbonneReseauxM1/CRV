name: build-image

on:
  push:
    branches:
      - 'master'

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: 21310174  
          password: 19001560Truc!
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: truckat_img
